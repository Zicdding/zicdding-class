
const fs = require("fs");

const uploadService = {
    upload : async (req,res) =>{
        try{
            const {originName, filename, mimeType, path} = req.file;

        }catch{

        }
    }
}

module.exports = uploadService;
