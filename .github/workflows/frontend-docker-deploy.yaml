name: CI/CD Pipeline

on:
  push:
    branches:
      - feat/front-docker-deploy

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          push: true
          context: frontend
          file: frontend/zicdding-class.com/Dockerfile
          tags: zicdbottest/zicdding-front:latest
#   deploy:
#     runs-on: ubuntu-latest
#     needs: build

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v2

#       - name: Set up kubectl
#         uses: azure/setup-kubectl@v1
#         with:
#           version: 'latest'

#       - name: Set up Kustomize
#         run: |
#           curl -Lo kustomize.tar.gz https://github.com/kubernetes-sigs/kustomize/releases/download/kustomize%2Fv4.0.5/kustomize_v4.0.5_linux_amd64.tar.gz
#           tar -xzf kustomize.tar.gz
#           sudo mv kustomize /usr/local/bin

#       - name: Deploy to Kubernetes
#         env:
#           KUBECONFIG: ${{ secrets.KUBECONFIG }}
#         run: |
#           kustomize edit set image ${{ secrets.DOCKER_USERNAME }}/my-app:latest
#           kubectl apply -k .
